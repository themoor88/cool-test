<% users.each do |user| %>
  <p><%= link_to String(user.id) + '. ' + user.name, messages_list_path(user_id: user.id) %></p>
<% end %>

$('.user-list').html('<%= escape_javascript( render partial: "/app/views/users/list", locals: { users: User.all })%>')
console.log("hello world");